#include <stdio.h>
#include <math.h>
int main() {
float p, r, t;
printf("Enter Principal, Rate, Time: ");
scanf("%f %f %f", &p, &r, &t);
float si = (p * r * t) / 100;
float ci = p * pow((1 + r / 100), t) - p;
printf("SI: %.2f\nCI: %.2f\n", si, ci);
return 0;
}
